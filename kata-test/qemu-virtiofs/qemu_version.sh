
get_qemu_repo(){
	local qemu_virtiofs_repo="https://github.com/rhvgoyal/qemu"
	echo "${qemu_virtiofs_repo}"
}

get_qemu_version(){
	local qemu_virtiofs_tag="thread-pool-zero"
	echo "${qemu_virtiofs_tag}"
}
